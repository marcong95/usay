<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge">
    <title><%= title %></title> 
    <link href="/common/lib/bootstrap-3.3.5-dist/css/bootstrap.min.css" rel="stylesheet">
    
    <% if(locals.toUploadFile){ %>
    <link href="/common/lib/bootstrap-fileinput-master20151222/css/bootstrap.min.css" rel="stylesheet">
    <link href="/common/lib/bootstrap-fileinput-master20151222/css/fileinput.css" media="all" rel="stylesheet" type="text/css" />
    <script src="/common/lib/bootstrap-fileinput-master20151222/js/jquery-2.0.3.min.js"></script>
    <script src="/common/lib/bootstrap-fileinput-master20151222/js/fileinput.js" type="text/javascript"></script>    
    <script src="/common/lib/bootstrap-fileinput-master20151222/js/fileinput_locale_zh.js" type="text/javascript"></script>
    <script src="/common/lib/bootstrap-fileinput-master20151222/js/bootstrap.min.js" type="text/javascript"></script>
    <% } %> 
    <link href="/user/css/common.css" rel="stylesheet">
</head>
    <nav class="navbar navbar-inverse" role="navigation">

    <div id="container" class="container">
     <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" 
           data-target="#navbar-collapse">
           <span class="sr-only">切换导航</span>
           <span class="icon-bar"></span>
           <span class="icon-bar"></span>
           <span class="icon-bar"></span>
        </button>
        <% if(locals.toBack){ %>
        <a class="navbar-brand glyphicon glyphicon-chevron-left" href="javascript:history.back()"><sup></sup></a>
        <% }else{ %>
        <a class="navbar-brand" href="/"><%= Upage.usay.label %><sup></sup></a>
        <% } %>
            
        <% if(locals.toSearch){ %>
        <div class="search-input">
          <div class="input-group">
            <input type="text" class="form-control search" placeholder="输入感兴趣的内容...">
            <a href="javascript:void(0)" onclick="toSearch()" class="input-group-addon"><i class="glyphicon glyphicon-search"></i></a>
          </div>
        </div>
        <% } %>
     </div>
     <div class="collapse navbar-collapse" id="navbar-collapse">        
        <ul class="nav navbar-nav">
          <% var topics = Upage.header.topics; %>
          <% for(var i in topics){ var topic = topics[i]; %><!-- for1 -->
           <li <% if(index==topic.label){ %> class="active" <% } %>><a href="<%= topic.link %>"><%= topic.name %></a></li>
          <% } %><!-- end for1 -->
        </ul>
        <ul class="nav navbar-nav navbar-right">
            <% var right = Upage.header.right; %>
            <% if(!locals.user){ %>
              <li <% if(index==right.login.label){ %> class="active" <% } %>><a href="<%= right.login.link %>"><%= right.login.name %></a></li>
              <li <% if(index==right.reg.label){ %> class="active" <% } %>><a href="<%= right.reg.link %>"><%= right.reg.name %></a></li>
            <% }else{ %>
              <li <% if(index==right.profile.label){ %> class="active" <% } %>><a href="<%= right.profile.link %>"><%= user.nickname?user.nickname:user.username %></a><input type="hidden" id="username" value="<%= user.username %>" ></li>
              <li <% if(index==right.logout.label){ %> class="active" <% } %>><a href="<%= right.logout.link %>"><i class="glyphicon glyphicon-log-out"></i></a></li>
            <% } %>
        </ul> 
     </div>
    </div>
  </nav>